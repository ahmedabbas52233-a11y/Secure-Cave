# ── API Base URL ──────────────────────────────────────────────────────
# Development: use /api (Vite proxy handles the rest)
# Production (different domain): https://your-backend.railway.app/api
VITE_API_URL=/api
